def df = "HH:mm:ss"
def mediodia = New Date().parse(df, "12:00:00")
def hora = new Date()

pipeline{
  agent any
  stages{
    stage("Hora"){
      steps{
        println "Son las: ${hora}. Y voy a comparar con ${mediodia}"
      }
    }
  }
}
